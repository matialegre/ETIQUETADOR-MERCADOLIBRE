# ===============================
# meli_stock_pipeline - .env
# ===============================
# Copiá este archivo como ".env" y completá las variables.
# Las rutas aceptan backslashes escapados (\\) o forward slashes (/).

# --- SQL Server ---
SQL_SERVER=.\\SQLEXPRESS
SQL_DB=meli_stock
# Para autenticación integrada Windows
SQL_TRUSTED=yes
# Si usás usuario/clave en lugar de Trusted_Connection
# SQL_TRUSTED=no
# SQL_USER=sa
# SQL_PASSWORD=TuPassword

# --- MercadoLibre Token ---
# Ruta al token.json. Recomendado: dejarlo dentro de ./config/token.json
# y apuntar acá. Podés dejar una ruta absoluta si preferís.
TOKEN_PATH=C:\\Users\\Mundo Outdoor\\CascadeProjects\\meli_stock_pipeline\\config\\token.json

# Si querés habilitar refresh automático de token
# ML_CLIENT_ID=
# ML_CLIENT_SECRET=

# --- Dragonfish ---
# Timeout para las consultas de stock (segundos)
DRAGON_TIMEOUT_SECS=180

# --- Movimiento ---
# Destino de movimientos (WOO o MELI). Actualmente operativo: WOO
MOVIMIENTO_TARGET=WOO

# --- Logging ---
# INFO por defecto. Usá DEBUG para mayor verbosidad.
LOG_LEVEL=INFO
